#include <stdio.h>
#include <sdlib.h>
#include <node.h>
Node *insertBST(Node *n, int data)
{
  if(n==NULL)
    return newNode(data);
  if(data < n->val)
    n->left = insert(n->left, data);
  else
    n->right = insert(n->right,data);
  return n;
}

Node *insertBT()
{
  Node *root = NULL;
  root = newNode(root,7);
  root->left = newNode

}
